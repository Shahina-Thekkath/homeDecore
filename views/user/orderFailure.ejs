<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
    <meta http-equiv="Pragma" content="no-cache" />
    <meta http-equiv="Expires" content="0" />

    <title>Order Failure - Ludus</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/orderSuccess.css">
    
</head>
<body>
    <main class="container">
        <!-- Breadcrumb -->
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item active" aria-current="page">Payment Failed</li>
            </ol>
        </nav>

        <div class="order-success">
            <h1 class="page-title text-center">Payment Failed!</h1>
            <p class="subtitle text-center">Unfortunately, your payment could not be processed.</p>
            <div class="text-center mb-4">
               <p class="text-danger fw-bold">Payment failed via Razorpay - No amount has been charged.</p>
            </div>

            
            <!-- Payment Failure Information -->
            <div class="row mb-4">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header bg-warning text-dark">
                            <h5 class="mb-0">Possible Reasons for Payment Failure</h5>
                        </div>
                        <div class="card-body">
                            <ul class="mb-0">
                                <li>Insufficient balance in your account</li>
                                <li>Network connectivity issues during payment</li>
                                <li>Bank server temporarily unavailable</li>
                                <li>Incorrect card details or expired card</li>
                                <li>Payment cancelled by user</li>
                                <li>Daily transaction limit exceeded</li>
                                <li>International transactions disabled</li>
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-header bg-info text-white">
                            <h5 class="mb-0">How to Resolve This</h5>
                        </div>
                        <div class="card-body">
                            <ul class="mb-0">
                                <li>Check your account balance</li>
                                <li>Verify your card details are correct</li>
                                <li>Ensure stable internet connection</li>
                                <li>Try a different payment method</li>
                                <li>Contact your bank if issue persists</li>
                                <li>Check if international payments are enabled</li>
                                <li>Wait a few minutes and try again</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Details -->
            <div class="order-details">
                <h2 class="address-title text-center">Order Summary</h2>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Product</th>
                                <th>Quantity</th>
                                <th>Price</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% order.products.forEach(product => { %>
                                <tr>
                                    <td><%= product.name %></td>
                                    <td><%= product.quantity %></td>
                                    <td class="price-text">₹<%= product.price.toFixed(2) %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan="2" class="fw-bold">Subtotal</td>
                                <td class="price-text">₹<%= order.totalAmount.toFixed(2) %></td>
                            </tr>
                            <tr>
                                <td colspan="2" class="fw-bold">Shipping</td>
                                <td class="price-text">₹0.00</td>
                            </tr>
                            <tr class="grand-total-row">
                                <td colspan="2" class="fw-bold">Grand Total</td>
                                <td class="fw-bold">₹<%= order.totalAmount.toFixed(2) %></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>

            <!-- Shipping Address -->
            <div class="shipping-address">
                <h2 class="address-title text-center">Shipping Address</h2>
                <address class="text-center">
                    <span class="customer-name"><%= order.shippingAddress.name %></span><br>
                    <%= order.shippingAddress.address %>,<br>
                    <%= order.shippingAddress.locality %>, <%= order.shippingAddress.state %>,<br>
                    <%= order.shippingAddress.country %>, <%= order.shippingAddress.pincode %>
                </address>
            </div>

           <div class="alert alert-warning text-center mb-4">
                <strong>Important:</strong> Your order has not been placed yet. You can retry payment from your Order List page or contact our support team for assistance.
            </div>

            <!-- Continue Shopping Button -->
            <div class="text-center">
                <a href="/" class="btn-continue">Continue Shopping</a>
            </div>
        </div>
    </main>

    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
    if (window.history && window.history.pushState) {
        window.history.pushState(null, null, window.location.href);
        window.onpopstate = function () {
            window.location.href = "/"; // or redirect to any safe page
        };
    }
</script>

</body>
</html>