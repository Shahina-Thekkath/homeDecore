<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Optional Custom CSS -->
     <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .login-container {
            max-width: 400px;
            width: 100%;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        .company-logo {
            text-align: center;
            margin-bottom: 20px;
        }
        .company-logo svg {
            max-width: 270px;
            height: auto;
        }
        .login-header {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }
        .login-btn {
            width: 100%;
            background-color: #ff6b35;
            border-color: #ff6b35;
            color: white;
        }
        .login-btn:hover {
            background-color: #e55a2e;
            border-color: #e55a2e;
        }
        .login-btn:focus, .login-btn:active {
            background-color: #d44d26;
            border-color: #d44d26;
            box-shadow: 0 0 0 0.2rem rgba(255, 107, 53, 0.25);
        }
        .form-control:focus {
            border-color: #ff6b35;
            box-shadow: 0 0 0 0.2rem rgba(255, 107, 53, 0.25);
        }
        .forgot-password-link {
            display: block;
            text-align: center;
            margin-top: 10px;
            color: #ff6b35;
        }
        .forgot-password-link:hover {
            color: #e55a2e;
        }
        .logo svg {
            max-height: 40px; /* Adjust based on your needs */
            width: auto;
        }
    </style>
</head>
<body>

<div class="login-container">
    <div class="company-logo">
        <%- include('../../views/partials/icons/icon-logo') %>
    </div>
    <div class="login-header">Admin Login</div>
    <h4 class="text-danger"><%=locals.message?message: ""%></h4>
    <form action="/admin" method="POST">
        <!-- Email input -->
        <div class="mb-3">
            <label for="email" class="form-label">Email address</label>
            <input type="email" class="form-control" id="email" name="email" required placeholder="Enter email">
             <p class="text-danger"><%=locals.invalidEmail? invalidEmail:""%></p>
        
        </div>
        <!-- Password input -->
        <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" class="form-control" id="password" name="password" required placeholder="Enter password">
            <p class="text-danger"><%=locals.passwordIncorrect? passwordIncorrect:""%></p>
        </div>
        <!-- Login button -->
        <button type="submit" class="btn btn-primary login-btn">Login</button>
        <!-- Forgot password link -->
        <!-- <a href="/admin/forgot-password" class="forgot-password-link">Forgot Password?</a> -->
    </form>
</div>

<!-- Bootstrap JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
